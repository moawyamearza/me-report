<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('favicon.ico') }}">
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        
    </head>
    

    <body>
        <header>
            <div>
            <image class="hero-image" src="{{ asset("img/rr.jpg") }}">
                <div class="hero-text">
                    <h1 style="font-size:50px">MOAWYA MEARZA</h1>
                    
                </div>
            </div>
        </header>
        <div class="sidenav">
            <nav>
                
                    <a href="{{ url("home") }}">Home</a>
                    <a href="{{ path("about") }}">About</a>
                    <a href="{{ path("report") }}">Report</a>
                    <a href="{{ path("card") }}">Card</a>
                
            </nav>
        </div>

        {% block content %}
        Default content
        {% endblock %}

        <div class="footer-basic">
            <footer>
                
                <p class="copyright">Moawya© 2022</p>
            </footer>
        </div>
    </body>
</html>
