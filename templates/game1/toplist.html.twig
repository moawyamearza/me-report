{% extends "base1.html.twig" %}

{% block title %}Card Play{% endblock %}

{% block content %}

<h1>Leaderboard</h1>

<table>
    <thead>
        <tr>
            <th>Namn</th>
            <th>Poäng</th>
        </tr>
    </thead>
    <tbody>
        {% for entry in scores %}
            <tr>
                <td>{{ entry.name }}</td>
                <td>{{ entry.score }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}